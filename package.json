{
  "name": "sna4slack",
  "version": "3.0.0",
  "scripts": {
    "postinstall": "npm install --prefix front-end && npm run build --prefix front-end"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.0.0"
  }
}
